'use strict'

import UserModel from '../models/user-model';
import Router from '../routers/router';
import Users from '../collections/users'

var UserView = Backbone.View.extend({
  template = _.template(['#inserthere'].html()),
})
